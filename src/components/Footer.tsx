import { Typography } from "@mui/material";

const Footer = () => {
  return (
    <Typography>
      Copyright &copy; {new Date().getFullYear()} To Do App.
    </Typography>
  );
};

export default Footer;
